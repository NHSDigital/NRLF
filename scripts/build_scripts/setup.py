import setuptools

setuptools.setup(
    name="build_scripts",
    version="0.1.0",
    description="",
    url="#",
    author="",
    install_requires=[],
    author_email="",
    packages=setuptools.find_namespace_packages(),
)
